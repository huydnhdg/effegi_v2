@model List<WebApplication.Models.E_ProductCate>
@{
    ViewBag.Title = "Đặt hàng";
}
<div class="row">
    <div class="container">
        <div class="top-products row">
            @foreach (var item in Model)
            {
                <div class="product-small col-md-3">
                    <div class="col-inner">
                        <div class="box">
                            <div class="box-image">
                                <div class="">
                                    <a href="/dat-hang/@item.Link">
                                        <img src="@item.Thumnails" />
                                    </a>
                                </div>
                            </div>
                            <div class="box-text text-center">
                                <div class="title-wrapper">
                                    @*<p class="category text-uppercase">Yesoul</p>*@
                                    <p class="name product-title text-uppercase">
                                        <a href="/dat-hang/@item.Link">@item.Name</a>
                                    </p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            }

        </div>
    </div>
</div>


